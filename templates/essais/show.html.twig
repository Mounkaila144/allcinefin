{% extends 'base.html.twig' %}

{% block title %}Commande{% endblock %}

{% block body %}
    <h1>Commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>
            <tr>
                <th>Panier</th>
                <td>{{ commande.panier ? commande.panier|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_essais_index') }}">back to list</a>

    <a href="{{ path('app_essais_edit', {'id': commande.id}) }}">edit</a>

    {{ include('essais/_delete_form.html.twig') }}
{% endblock %}
