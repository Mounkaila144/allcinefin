{% extends 'base.html.twig' %}

{% block title %}pub{% endblock %}

{% block body %}
    <h1>pub</h1>

    <table class="table">
        <tbody>

    </table>
<a href="{{ path('admin_pub_index') }}"><button type="submit" class="btn btn-primary me-2">Moifier</button></a>
    Cancel
    <a href="{{ path('admin_pub_index') }}"><button class="btn btn-danger">retourt</button></a>
    {{ include('admin/pub/_delete_form.html.twig') }}
{% endblock %}
