{% extends 'base.html.twig' %}

{% block title %}manga{% endblock %}

{% block body %}
    <h1>manga</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ manga.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ manga.nom }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ manga.genre }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ manga.type ? manga.type|join(', ') : '' }}</td>
            </tr>
            <tr>
                <th>Saison</th>
                <td>{{ manga.saison }}</td>
            </tr>
            <tr>
                <th>Episode</th>
                <td>{{ manga.episode }}</td>
            </tr>
        </tbody>
    </table>
<a href="{{ path('admin_manga_index') }}"><button type="submit" class="btn btn-primary me-2">Moifier</button></a>
    Cancel
    <a href="{{ path('admin_manga_index') }}"><button class="btn btn-danger">retourt</button></a>
    {{ include('admin/manga/_delete_form.html.twig') }}
{% endblock %}
