{% extends 'base.html.twig' %}

{% block title %}film{% endblock %}

{% block body %}
    <h1>film</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ film.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ film.nom }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ film.genre }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ film.type ? film.type|join(', ') : '' }}</td>
            </tr>
            <tr>
                <th>Saison</th>
                <td>{{ film.saison }}</td>
            </tr>
            <tr>
                <th>Episode</th>
                <td>{{ film.episode }}</td>
            </tr>
        </tbody>
    </table>
<a href="{{ path('admin_film_index') }}"><button type="submit" class="btn btn-primary me-2">Moifier</button></a>
    Cancel
    <a href="{{ path('admin_film_index') }}"><button class="btn btn-danger">retourt</button></a>
    {{ include('admin/film/_delete_form.html.twig') }}
{% endblock %}
